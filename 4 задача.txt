#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
	int x, a, b, c, d, i, z;
	cin >> x;
	b = x % 10;
	a = x / 10;
	c = a + b;
	d = a * b;
	i = min(c, d);
	z = max(c, d);
	while (i <= z) {
		cout << i << " ";
		i++;
	}
	return 0;
}