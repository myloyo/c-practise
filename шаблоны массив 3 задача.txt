#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

template <typename x>
void vvodvivod(int n) {
    x* a = new x[n];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    for (int i = 0; i < n; i++) {
        cout << a[i] << " ";
    }
}
int main() {
    int n;
    cin >> n;
    vvodvivod(n);
    return 0;
}
